# むしトリたいかい スコア記録ツール 設計書

イベント「夏休みむしトリたいかい！〜ルチャブル編〜」（ポケモンスリープ）用の
個人向けスコア記録ツールの設計をまとめたもの。

---

## 1. 目的・背景

- イベント期間中、日々のリサーチ結果を手元に記録しておきたい
- 最終的に **一番スコアが高かったリサーチはどれか** をひと目で把握したい
- スコア計算のルールが複雑（種族ボーナス・色違い・スプレー）なので、手計算をやめて自動化したい

「合計点を競う」のではなく **1リサーチの最高得点を競う** イベントなので、
週間合計は表示しない。

## 2. 利用者・利用シーン

| 項目 | 内容 |
|---|---|
| 利用者 | 依頼者本人 1名のみ |
| 端末 | 主にスマートフォンのブラウザ |
| 利用タイミング | リサーチ完了後、X（Twitter）へ投稿する前 |
| 複数人での共有 | **なし**（他人のスコアと突き合わせる機能は作らない） |

利用者が1名のため、サーバやデータベースは用意しない。
ブラウザ内（localStorage）にのみ保存する。

## 3. 機能要件

### F1. 日別のリサーチ記録

- DAY1〜DAY7 を用意する
- **1日あたり2枠**（①・②）を固定で設ける（1日に2回リサーチすることがあるため）
- 使わなかった枠は空のままでよい（スコア0として扱い、ベストスコア判定の対象外）

### F2. ポケモンの入力とスコア自動計算

- 1つのリサーチにつき **最大10体** まで登録できる
- 1体ごとに「ポケモン名」＋「色違いかどうか」を指定する
- 入力を変更するたびにスコアを即座に再計算して表示する
- 同じポケモンを複数体登録できる（例：ワシボン×2）

### F3. むしよけスプレー

- リサーチ単位で「スプレー使用」を指定できる
- **イベント期間中1回だけ** 使用可能。1箇所で使用したら、他の枠のスプレーは選べなくする
- 使用を取り消せば、再び他の枠で使えるようにする

### F4. X投稿用の出力

- 写真を選択すると、その写真に **スコアを重ねた合成画像** を生成する
- あわせて **投稿文（ハッシュタグ・DAY・スコアを含むテキスト）** を自動生成する
- 生成した画像はダウンロード、投稿文はコピーができる
- **写真そのものは保存しない**（容量の都合。合成画像もその場限りで、再読み込みすると消える）

### F5. ベストスコア表示

- 全14枠（7日 × 2枠）の中で最もスコアが高いリサーチを、画面上部に常に表示する
- 表示内容：DAY・枠番号・スコア

### F6. 週間合計

- **表示しない**（イベントの競技ルール上不要なため）

---

## 4. スコア計算ルール

### 4.1 個体スコア

各ポケモンに固定の点数を持たせる。

**加点（ひこう系）**

| ポケモン | 点 | ポケモン | 点 |
|---|---|---|---|
| ワシボン | +5 | ウォーグル | +12 |
| チルット | +5 | チルタリス | +12 |
| ドードー | +5 | ドードリオ | +12 |
| ネイティ | +5 | ネイティオ | +12 |
| ウッウ | +16 | デリバード | +16 |
| カモネギ | +16 | ヤミカラス | +16 |
| ルチャブル | +26 | ドンカラス | +20 |
| リザードン | **+40** | ボーマンダ | +25 |
| クレセリア | +30 | | |

**減点（むし系）**

| ポケモン | 点 | ポケモン | 点 |
|---|---|---|---|
| アゴジムシ | -5 | デンヂムシ | -12 |
| クワガノン | -22 | イシズマイ | -5 |
| イワパレス | -12 | ヘラクロス | **-32** |
| ツボツボ | -16 | カイロス | -16 |
| キャタピー | -5 | トランセル | -7 |
| バタフリー | -15 | | |

**0点だが選択肢に出すもの**

ヒトカゲ / リザード / タツベイ / コモルー
（個体としては加点されないが、種族ボーナスの判定に必要なので登録できるようにする）

> **リザードン+40 / ヘラクロス-32 について**
> イベント告知にある「天敵ボーナス（リザードン・ヘラクロスは得点が倍）」は、
> **すでにこの数値に反映済み**（+20→+40、-16→-32）。
> したがってプログラム側で別途2倍する処理は行わない。

### 4.2 色違い

- 色違いの場合、**その個体のスコアを2倍**にする（加点・減点どちらも）
- **種族ボーナスには適用しない**（固定値のまま）

### 4.3 種族ボーナス

同じ系統のポケモンが全形態そろった場合に、固定点を加算/減算する。
各形態が1体以上いれば成立する（2体いても重複加算はしない）。

**加点**

| 条件 | 点 |
|---|---|
| ワシボン ＋ ウォーグル | +10 |
| チルット ＋ チルタリス | +10 |
| ドードー ＋ ドードリオ | +10 |
| ネイティ ＋ ネイティオ | +10 |
| ヤミカラス ＋ ドンカラス | +10 |
| ヒトカゲ ＋ リザード ＋ リザードン | +20 |
| タツベイ ＋ コモルー ＋ ボーマンダ | +20 |

**減点**

| 条件 | 点 |
|---|---|
| イシズマイ ＋ イワパレス | -10 |
| アゴジムシ ＋ デンヂムシ ＋ クワガノン | -20 |
| キャタピー ＋ トランセル ＋ バタフリー | -20 |

### 4.4 むしよけスプレー

- そのリサーチ内の **減点のうち、一番大きい1つだけ** を0にする
- 対象は「むしポケモンの個体減点（色違い2倍を適用した後の値）」と「むし系の種族ボーナス減点」の両方
- 使えるのは **イベント期間中1回のみ**

### 4.5 計算の順序

1. 各個体のスコアを求める（色違いなら2倍）
2. 種族ボーナスを加算/減算する（色違いによる倍化はしない）
3. スプレー使用時、減点項目のうち絶対値が最大のもの1つを0にする
4. 合計をそのリサーチのスコアとする

**計算例（色違いアゴジムシ系がそろった場合／スプレーなし）**

| 項目 | 計算 | 点 |
|---|---|---|
| アゴジムシ（色違い） | -5 × 2 | -10 |
| デンヂムシ（色違い） | -12 × 2 | -24 |
| クワガノン（色違い） | -22 × 2 | -44 |
| 種族ボーナス | 固定（倍化しない） | -20 |
| **合計** | | **-98** |

### 4.6 DAY7限定ルール

最終日（DAY7）だけ、ポッチャマ系がトリとして追加される。あわせてツボツボの減点が変わる。

**DAY7でのみ選択できるポケモン**

| ポケモン | 点 |
|---|---|
| ポッチャマ | +5 |
| ポッタイシ | +12 |
| エンペルト | **+40** |

**DAY7でのみ変わる点数**

| ポケモン | 通常 | DAY7 |
|---|---|---|
| ツボツボ | -16 | **-32** |

**DAY7でのみ成立する種族ボーナス**

| 条件 | 点 |
|---|---|
| ポッチャマ ＋ ポッタイシ ＋ エンペルト | +20 |

> **エンペルト+40 / ツボツボ-32 について**
> エンペルトは みず／はがね で、むし／いわ のツボツボに強い。
> 4.1 のリザードン+40 / ヘラクロス-32 と同じ扱いで、
> **天敵ボーナスを反映済みの数値**とする（プログラム側で別途2倍しない）。

DAY1〜6では、ポッチャマ系は選択肢に出さず、種族ボーナスも成立しない。
ツボツボは通常どおり-16のまま。

---

## 5. データ設計

localStorage にJSON形式で1件だけ保存する（キー：`mushitori_v1`）。

```json
{
  "version": 1,
  "days": [
    {
      "day": 1,
      "slots": [
        {
          "label": "リサーチ名",
          "spray": false,
          "mons": [
            { "name": "リザードン", "shiny": false },
            { "name": "ヘラクロス", "shiny": true }
          ]
        },
        { "label": "", "spray": false, "mons": [] }
      ]
    }
  ]
}
```

- スコアは保存しない（入力内容から毎回計算する）
- 写真・合成画像は保存しない
- 「スプレーを使ったか」は各枠の `spray` を全走査して判定する（別途フラグは持たない）

> **旧バージョンからの変更点**
> 現行の `mushi_tori_complete.html` は `document.body.innerHTML` をそのまま
> localStorage に保存している。写真がBase64で丸ごと含まれるため容量上限に
> 達しやすく、HTMLの構造を変えると過去データが壊れる。
> 新設計では「入力値だけをJSONで持つ」方式に変更する。

---

## 6. 画面設計

```
┌──────────────────────────────┐
│ ★ ベストスコア  DAY3 ②  128点      │ ← 画面上部に固定表示
├──────────────────────────────┤
│ ▼ DAY1                            │
│   ① [リサーチ名____]  スコア: 42点  │
│      □ むしよけスプレー             │
│      [ポケモン選択▼] □色違い  [削除] │
│      [ポケモン選択▼] □色違い  [削除] │
│      [＋ポケモン追加]  (2/10)       │
│      [写真を選ぶ] → プレビュー       │
│      [投稿用画像を作る] [投稿文コピー]│
│   ② （同じ構成）                    │
│ ▼ DAY2 …                          │
└──────────────────────────────┘
```

- DAY見出しは折りたたみ可能にして、スマホでも縦に長くなりすぎないようにする
- スプレー使用済みのときは、他の枠のチェックボックスを無効化し「使用済み」と表示する
- ポケモンが10体に達したら「＋ポケモン追加」を無効化する

---

## 7. 技術構成

| 項目 | 内容 |
|---|---|
| 形式 | HTML1ファイル（CSS・JavaScript を内包） |
| フレームワーク | なし（素のJavaScript） |
| 保存先 | ブラウザの localStorage |
| 画像合成 | Canvas API |
| 公開方法 | GitHub Pages |

### GitHub Pages について

GitHub Pages は、リポジトリに置いたファイルをそのままWebに公開する仕組み。
`https://<ユーザー名>.github.io/<リポジトリ名>/<ファイル名>` でアクセスできる。

サーバ側でプログラムを動かすことはできないが、JavaScriptはブラウザ上で動くため
このツールは問題なく動作する。今回は1人用でデータ共有が不要なので、この構成で足りる。

---

## 8. 制約・注意事項

- データは **使ったブラウザの中にしか残らない**。別の端末や別のブラウザでは見られない
- ブラウザの履歴・サイトデータを削除すると記録も消える
- 写真は保存しないため、ページを再読み込みするとプレビューは消える
- X への投稿は手動（自動投稿はしない／できない）

---

## 9. 判断が必要だった論点と決定

資料（告知画像・計算例テキスト）だけでは読み取れなかった点、および
資料どうしで記載が食い違っていた点。すべて確認済みで、決定内容は本書に反映済み。

| # | 論点 | 決定 |
|---|---|---|
| 1 | イベント期間の日数（告知に「10日1回目のリサーチ〜」とあるが終了日の記載なし） | **7日間** |
| 2 | スプレーで0にする「一番大きい減点」に、むし系の種族ボーナス減点も含めるか | **含める** |
| 3 | 色違いボーナスを種族ボーナスにも適用するか（告知は「＋も−も全て2倍」、計算例の-98とは食い違う） | **適用しない**。計算例の-98が正しい |
| 4 | スプレー使用例のヘラクロスが-16表記（個体スコア表は-32） | **-16が誤り。-32を使用する** |
| 5 | ヤミカラス＋ドンカラスの種族ボーナス（計算例テキストに記載なし） | **適用する（+10）** |

### 参考資料

- `S__126705705.jpg` … イベント告知画像
- `ポケモンスコア例・計算例.txt` … 依頼者から提供された点数表と計算例
